// Solidity
pragma solidity >=0.8.2 <0.9.0;

// contract
contract SimpleContract{
    // Arithmetic operators +, -, *, /, %, **, ++, --
    uint a = 2;
    uint b = 3;
    uint c = a + b; // 5

    // Logical operators &&, ||, !
    bool hasMoney = !false;

    // Bitwise operators &, |, ^, ~, <<, >>
    bytes1 a = contractValue Ox01 | Ox02; 
}